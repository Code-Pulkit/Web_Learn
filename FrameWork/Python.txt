List are mutable and slower. []
Tuples are immutable and faster. ()
Sets are unique. {}
A dictionary stores values in key and value pairs where each value can be accessed through its key. {key : value}

Python is an interpreted Language - No need to compile before Running.
Writing Python code is quick but running it is often slower than compiled languages.
Python is well suited to object orientated programming in that it allows the definition of classes along with composition and inheritance.
Dynamically Typed - 

Decorators are used to add some design patterns to a function without changing its structure.

Slicing in Python - (In List , Tuples and Strings )
  1. [start:end:step] ==> 
    If the start or end element is negative i, it means the ith element from the end.
    

